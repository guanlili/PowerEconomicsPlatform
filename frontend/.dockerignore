# 不要把宿主机的 node_modules 复制进 build context
# （当前 Dockerfile 不在容器内 build，不需要 node_modules，也不需要源码以外的文件）
node_modules
dist-ssr

# 源码相关，docker build 不需要
src
public
index.html
package*.json
tsconfig*.json
vite.config.ts
eslint.config.js
build-dist.sh
README.md
.gitignore

# 日志
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# IDE / 系统
.vscode
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Git
.git
.gitignore

# 之前的离线包/构建产物 tar
power-economics-platform-frontend-*.tar
*.tar
